# Required in production. Used to encrypt template and form data at rest.
ARTIFACT_ENCRYPTION_KEY=replace-with-a-long-random-secret

# Optional. Directory where the SQLite database file is stored.
# Defaults to the current working directory in local development.
DATA_DIR=./data
